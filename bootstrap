#!/usr/bin/env bash

ask () {
  printf "\n"
  read -p "$1 " -r
}

info () {
  printf "\n*** $1 ***\n"
}

info 'Installing Homebrew'
  /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
  brew update

info 'Installing Homebrew packages'
  brew install git heroku-toolbelt Schniz/tap/fnm vim zsh-syntax-highlighting

info 'Setting zsh as default shell'
  chsh -s /bin/zsh

ask 'Which Node version do you want to install?'
  fnm install "$REPLY"
  fnm default "$REPLY"

info 'Installing Yarn'
  npm install -g yarn

info 'Installing Pure prompt'
  npm install -g pure-prompt

info 'Symlinking dotfiles'
  sh symlink

info 'Install vim-plug'
  curl -fLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

info 'Done! Please logout for changes to take effect.'
